import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.minhamorada.app',
  appName: 'Minha Morada',
  webDir: 'dist/APP/browser',
  backgroundColor: '#edf8ff',
};

export default config;
